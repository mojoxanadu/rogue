rogue-notes.txt

# Binary assets — copied manually by design team, not tracked.
# See BUILDS.md for the rationale (LFS unavailable on the current git server).
raw/
__pycache__/
*.pyc

# Build artifacts — roguelike.html (the release output) IS tracked.
dev_build.html
assets.json
err
log

# PWA bundle (derived from dev_build.html via mobile/Makefile).
mobile/dist/

# Local-only credentials + ops scripts (intentionally not secured — throwaway PAT for GitHub mirror).
GITHUB_TOKEN
snapshot

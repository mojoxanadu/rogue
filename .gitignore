rogue-notes.txt

# Source asset staging dir — populate from upstream, not tracked.
assets/
raw/
__pycache__/
*.pyc

# Build artifacts — roguelike.html (the release output) IS tracked.
dev_build.html
assets.json
roguelike_assets*.dat

# PWA bundle (derived from dev_build.html via mobile/Makefile).
# NOTE: mobile/dist/ IS tracked — testers can clone the repo on
# Termux/Android and serve straight from dist/ without needing python
# + make to rebuild. Ungitignored deliberately.

# Local-only credentials + ops scripts (intentionally not secured — throwaway PAT for GitHub mirror).
GITHUB_TOKEN
.envrc
snapshot

# Per-developer Claude Code state (settings.local.json, scheduler locks).
.claude/
